# Lotto Number Generator (로또 번호 생성기)

## Overview

배포·수익화가 가능한 수준의 한국형 로또 6/45 번호 생성 웹앱. 프레임워크 없이 HTML/CSS/JS로 구현.

## Implemented Features

### v1 (초기 버전)
- 1~45 중 6개 무작위 생성, 버튼 클릭 시 표시.

### v2 (현재 버전)
- **다중 게임 생성**: 1~5게임 동시 생성 (A~E 라벨).
- **생성 모드**: 완전 랜덤 / 홀짝·구간 균형 모드.
- **고정수·제외수**: 쉼표 구분 입력, 유효성 검사와 한국어 오류 메시지.
- **번호 저장**: localStorage에 최대 50개 저장, 개별/전체 삭제.
- **복사·공유**: 클립보드 복사, Web Share API(미지원 브라우저는 복사로 폴백).
- **디자인**: 다크 프리미엄 테마, 동행복권 공식 볼 색상(노랑/파랑/빨강/회색/초록), 등장 애니메이션, 토스트 알림, 모바일 반응형.
- **수익화 준비**: 상단/하단 광고 슬롯(`#ad-top`, `#ad-bottom`), SEO/OG 메타태그.
- **접근성**: aria-live 결과 영역, role 지정, 키보드 조작 가능한 컨트롤.
- **법적 고지**: 당첨 보장 불가 및 확률 고지 푸터.

## Style & Design

- 컬러: 다크 배경(#101418) + 골드 액센트(oklch), 볼 색상은 공식 로또 색 체계.
- 타이포: Pretendard/Apple SD Gothic Neo 시스템 폰트 스택.
- 컴포넌트: 세그먼트 버튼, 카드 레이아웃, 다층 그림자, pop 애니메이션.

---

# 자모 사과게임 (`/jamo`)

## Overview

사과게임(Fruit Box) 형식의 한글 단어 퍼즐. 같은 저장소에서 운영하는 두 번째 무료 웹앱이며,
로또 생성기와 상호 링크로 페이지뷰를 주고받는다. 역시 프레임워크 없는 정적 HTML/CSS/JS.

## Core Rule

드래그한 **사각형 안의 자모를 전부 사용해** 단어를 만들 수 있으면 제거된다. **순서는 무관.**
사과게임의 "합이 10"이 교환법칙을 따르는 것처럼, 여기서는 **애너그램 판정**으로 같은 성질을 만든다.

- 판에는 **기본 24자모만** 올라간다. 겹자음·복합모음은 분해된다 (`ㅐ`=`ㅏ`+`ㅣ`, `ㅘ`=`ㅗ`+`ㅏ`, `ㄲ`=`ㄱ`+`ㄱ`).
- 제거된 칸은 **빈칸으로 남고**, 이후 사각형은 빈칸을 건너뛰고 자모를 묶는다 (사과게임과 동일).
- 한 번에 묶을 수 있는 자모는 2~6개.

## Implementation Notes

- **애너그램 색인**: 단어를 자모 분해 후 정규 순서로 정렬한 문자열을 키로 `Map`에 넣는다.
  드래그 판정은 선택 영역의 자모 개수 배열 → 같은 키 → `O(1)` 조회.
- **판 생성**: 무작위로 자모를 뿌리면 만들 수 있는 단어가 거의 없다. 그래서 **실제 단어를
  직사각형 모양으로 깔고 그 안에서 섞는** 방식으로 시작 시점 해답 수를 보장한다 (평균 약 120개).
  자투리 칸만 자모 빈도에 맞춰 채운다.
- **해답 탐색** (`findSolutions`): 열 구간을 고정하고 행을 늘려가며 자모 수가 6을 넘으면 즉시 가지치기.
  전체 스캔이 1ms 미만이라 매 제거마다 호출해 "더 이상 만들 단어 없음"을 판정하고 힌트에도 재사용한다.
- **오늘의 판**: 날짜(KST) 해시를 시드로 `mulberry32`를 돌려 전 세계가 같은 판을 푼다. 서버 불필요.

## Features

- 2분 타이머, 점수(`n + max(0, n-3)*2` — 긴 단어일수록 보너스), 힌트 3회
- **콤보**: 직전 제거로부터 3초 안에 또 지우면 배수가 붙는다 (×1.5 → ×2 → ×2.5, ×3에서 상한).
  HUD 중앙에 남은 시간 막대를 띄워 창이 열려 있다는 걸 보이게 한다.
- **쉬움 모드**: 지금 만들 수 있는 단어를 판 아래에 전부 띄우고, 지울 때마다 목록에서 없앤다.
  매 제거마다 다시 계산하므로 불가능해진 단어가 남거나 새로 가능해진 단어가 빠지지 않는다.
  최고 점수는 일반 모드와 따로 적는다.
- 오늘의 판(하루 1회 기록) / 연습 모드(무한 랜덤)
- 최고 점수·연속 출석 `localStorage` 저장, 결과 공유 텍스트(Web Share API, 미지원 시 복사)
- 상단/하단 광고 슬롯, SEO/OG 메타태그, 로또 생성기와 상호 링크

## Files

- `jamo/index.html` — 시작·게임·결과 세 화면
- `jamo/jamo.js` — 자모 분해, 사전 색인, 판 생성, 해답 탐색, 드래그 판정 (Node에서 코어만 `require` 가능)
- `jamo/jamo.css` — 로또 생성기와 같은 다크+골드 팔레트
- `jamo/words.js` — 단어 사전(일상 명사). 자모 2~6개짜리만 로드 시점에 채택 (현재 659개)

## Current Task

- 로또 생성기 v2 완료. 다음 단계는 `LAUNCH_PLAN.md`의 7일 런칭 플랜 실행 (배포 → 광고 승인 → SEO → 트래픽).
- 자모 사과게임 v1 완료 (쉬움 모드·콤보 포함). 사전 단어를 늘리면 판의 밀도와 재미가 함께 올라간다.
